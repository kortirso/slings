= render 'components/page/page', path: :catalog do
    = render 'components/category/category', category: @category do
        = render 'components/categories_nav/categories_nav', categories: @categories, active_category: @category.friendly_id
