.columns.small-12.medium-3.large-2
    .catalog_nav
        - @categories.each do |category|
            = link_to category.name, category_path(category), class: "button #{'active' if params[:id].try{ |id| id == category.friendly_id }}"